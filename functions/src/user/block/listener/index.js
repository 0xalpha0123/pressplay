/**
 * Handles functions when creating a blocked user record
 */
exports.create = require("./create");

/**
 * Handles functions when deleting a blocked user record
 */
exports.delete = require("./delete");
